# sphere-digit-renderer
Rotating 3D sphere rendered with OpenGL (WinAPI) where the surface is covered by animated digits. Uses bitmap fonts, orthographic projection, and real-time rotation — built with C++ and legacy OpenGL on Windows.
